procfile:
  web:
    command: ./start.sh
    hosts:
      - button.lvh.me
    # Serves a realtime hub at http://button.lvh.me/socketio; open /socketio/_test for the live test.
    pubsub:
      path: /socketio
      test: true
    health: /up
autostart: button
